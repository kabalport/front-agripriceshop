// src/components/common/UserHeader.tsx

import React from 'react';

const UserHeader: React.FC = () => {
    // Render user-specific header
    return (
        <header>
            {/* Your user header elements here */}
            <h1>사용자 헤더</h1>
        </header>
    );
};

export default UserHeader;
